import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  button: {
    alignItems: "center",
    backgroundColor: "#103665",
    borderRadius: 10,
    height: 50,
    justifyContent: "center",
    width: "100%",
  },
  buttonText: {
    color: "white",
  },
  pickersContainer: {},
});
